// Private ETC history is restored after sign-in from a local backup.
export const ETC_SOURCE_STATS = {
  rawCount: 0,
  uniqueCount: 0,
  duplicateCount: 0,
  excludedCount: 0
};

export const ETC_RECORDS = [];
